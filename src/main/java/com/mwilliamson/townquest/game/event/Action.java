package com.mwilliamson.townquest.game.event;

// Note to self - may need to add ActionRequest for anything that is verified/performed by the server
public enum Action
{
    MOVE,
    REQUEST_QUEST,
    GET_WOOD
}
